{
  "name": "deco-brick-starter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": "concurrently --handle-input \"wait-on ./dist/index.js && nodemon\" \"tsc -w -p tsconfig.json\" ",
    "start": "node dist/index.js",
    "watch": "tsc -w -p tsconfig.json",
    "dev": "nodemon ./dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/joi": "^13.6.0",
    "deco-brick": "^0.1.6",
    "joi": "^13.7.0"
  },
  "devDependencies": {
    "@types/node": "^10.11.5",
    "concurrently": "^5.0.0",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.1",
    "typescript": "^3.7.2",
    "wait-on": "^3.3.0"
  }
}
